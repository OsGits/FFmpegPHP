
## 更新内容

### update.php - 优化在线更新功能的备份逻辑

**问题描述：**
在线更新功能进行备份时，会将 `data`、`m3u8`、`vodoss`、`ZmFinish` 等数据目录一起备份，导致备份文件过大。

**修复内容：**
修改 `create_backup()` 函数中的备份逻辑，在备份时跳过以下目录：

- `data` - 整个数据目录
- `m3u8`
- `vodoss`
- `ZmFinish`

同时优化了目录检测逻辑，使用 `DIRECTORY_SEPARATOR` 确保跨平台兼容性。
